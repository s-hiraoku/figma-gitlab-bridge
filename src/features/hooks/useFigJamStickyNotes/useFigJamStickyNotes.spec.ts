import { renderHook, act } from "@testing-library/react";
import { useFigmaStickyNotes } from "./useFigJamStickyNotes";

describe("useFigmaStickyNotes test", () => {
  test("should xxx", () => {
    const { result } = renderHook(() => useFigmaStickyNotes());

    // eslint-disable-next-line @typescript-eslint/no-empty-function
    act(() => {});

    expect().toBe();
  });
});
